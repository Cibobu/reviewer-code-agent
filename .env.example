# --- LLM (OpenAI-compatible) -------------------------------------------------
LLM_BASE_URL=https://api.openai.com/v1
LLM_API_KEY=
LLM_MODEL=gpt-4o-mini

# --- GitHub (optional — private repos or higher rate limits) -----------------
GITHUB_TOKEN=

# --- HTTP server -------------------------------------------------------------
PORT=9004
